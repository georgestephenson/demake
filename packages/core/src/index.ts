/**
 * Public API of `@demake/core`.
 *
 * Phase 0 exposes only a hello-world surface. The real engine API — `prep`,
 * `gen`, `inspect`, `consoles`, and the console/codegen types (doc 09) — lands
 * in Phase 1 and beyond.
 */
export { greeting } from "./greeting.js";
export { CORE_VERSION } from "./version.js";
