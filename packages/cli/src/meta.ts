/**
 * CLI version string.
 *
 * A hand-maintained constant kept in sync with `packages/cli/package.json`.
 * Release tooling (Changesets, doc 11) becomes the source of truth in a later
 * phase. The command surface itself now lives in `@demake/cli-spec` (doc 05).
 */
export const CLI_VERSION = "0.0.0";
