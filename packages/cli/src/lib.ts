/**
 * Library entry point for the `demake` package.
 *
 * Per doc 09, installing `demake` alone is enough to script against the engine:
 * the CLI package re-exports the full `@demake/core` surface.
 */
export * from "@demake/core";
