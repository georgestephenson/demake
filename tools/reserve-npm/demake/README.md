# demake

**This name is reserved.** `demake` converts any image into hardware-compliant
art — and displayable code — for 8/16-bit-era consoles and handhelds up to the
Nintendo DS.

This is a placeholder release that holds the name while the project is built in
the open. The real CLI and library are on the way.
