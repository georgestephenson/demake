---
---

Name the game language **Demotic** (`.dmt`), rename `packages/game` to
`packages/demotic`, and plan the full concept across the doc set: doc 14 (the
language) and new doc 15 (the Demakefile), with amendments to architecture, CLI,
codegen, web app, library API, testing, and roadmap.

No release: `@demake/demotic` is unpublished, and no published package's output
bytes change.
