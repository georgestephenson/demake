node_modules/
dist/
coverage/
pnpm-lock.yaml
*.tsbuildinfo

# Design docs are hand-authored prose (tables, deliberate wrapping); Prettier's
# Markdown reflow is not wanted here. They remain the source of truth as written.
docs/

# Assembly is column-aligned by hand and Prettier has no parser for it; the
# `.inc` extension is the only reason it tries. `runtime-harness/` is the
# Demotic engine, `rom-harness/` the image display programs.
runtime-harness/
rom-harness/

# Generated: `pnpm gen:runtime` writes it, and a test checks it is current.
packages/demotic/src/rom/runtime-gb.generated.ts
